import React, { useState } from 'react';

const SubHeader = () => {
    const [banner, setbanner] = useState('');

    return (
        <div>
            {
                
            }
        </div>
    );
};

export default SubHeader;